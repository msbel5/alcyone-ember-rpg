namespace EmberCrpg.Domain.Time
{
    /// <summary>Calendar season label. Systems use data rows for behavior instead of branching on this enum.</summary>
    public enum Season
    {
        None = 0,
        Spring = 1,
        Summer = 2,
        Autumn = 3,
        Winter = 4,
    }
}
