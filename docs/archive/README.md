# docs/archive — historical, not current

Audit items EMB-050 / EMB-022. This folder holds **historical** material moved out of the active
tree so current-state discovery is fast. None of it is canonical — for the live state read
`docs/CURRENT_STATE.md`, for the cleanup tracker `docs/AUDIT_COUNTER.md`.

- `sprint/` — 156 Godot-era / early-Unity sprint + atom-map + acceptance docs (sprint-*, faz-* per-atom).
- `reports/` — 102 build logs, screenshots, diffs, subagent outputs, and proof artifacts from prior runs.
- `plans/` — planning docs moved out of `Assets/` (EMB-023).

Kept (not deleted) to preserve history. The `git mv` retains blame/history; internal relative links
between archived docs still resolve. A few legacy references from `README.md` / `agent-rules-v2.md`
were repointed here.
